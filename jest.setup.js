// Set up any global variables needed for testing
global.d3 = require('d3');

// Add any custom matchers or global test setup here
expect.extend({
  // Add custom matchers for D3/SVG testing if needed
});
